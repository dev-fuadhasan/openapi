{
  "name": "open-api-exposure-scanner-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Open API Exposure Scanner",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

